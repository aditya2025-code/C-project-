Q. A problem where the output is only YES/NO is best classified as:
A) Optimization problem
B) Decision problem
C) Search problem
D) Enumeration problem

2

Q. A problem instance is:
A) A general form of a problem
B) A specific input case of a problem
C) A programming language feature
D) A data structure

2

Q. Requirement:Create a program to calculate a students final grade. Which clarification
question is most essential?
A) Which IDE should be used?
B) What is the student's name?
C) What are the input components and their weightage (marks/percentage)?
D) Should the output be in color?

3

Q. For Compute average of N numbers, a valid pre-condition is:
A) Output must be printed
B) N > 0
C) Average must be integer
D) Numbers must be sorted

2

Q. Algorithm correctness means:
A) Uses minimum memory always
B) Produces correct output for all valid inputs
C) Runs fastest among all algorithms
D) Uses recursion

2

Q. Which algorithm is asymptotically more efficient for large n?
A) O(n^2)
B) O(n log n)
C) O(n^3)
D) O(2^n)

2

Q. Role of data structures in problem solving is mainly to:
A) Increase program file size
B) Organize data for efficient access and updates
C) Replace algorithms
D) Avoid input/output

2

Q. A program reads age. Which is the best input validation condition?
A) age != 0
B) age >= 0 && age <= 120
C) age % 2 == 0
D) age < 1000 only

2

Q. ASCII value of character 'A' is:
A) 97
B) 65
C) 48
D) 66

2

Q. Which loop is exit-controlled?
A) for
B) while
C) do-while
D) none

3

Q. Which is best for sentinel-controlled repetition (unknown count, stop at -1)?
A) for(i=0;i<n;i++)
B) while(x!=-1)
C) switch(x)
D) if(x==-1)

2

Q. What is the output?
int i,j,sum=0;
for(i=1;i<=3;i++)
{for(j=1;j<=i;j++) 
sum += j;}
printf("%d", sum);
A) 6
B) 10
C) 12
D) 9

2

Q. Which C statement correctly reads an integer x?
A) input(x);
B) read x;
C) scanf("%d",&x);
D) cin >> x;

3

Q. C is mainly a:
A) Typeless language
B) Strongly typed language
C) Markup language
D) Query language

2

Q. For x = -2, what prints?
if (x > 0)printf("P");
printf("Q");
A) P only
B) Q only
C) PQ
D) Nothing

2

Q. In 8-bit 2's complement, adding 01111111 (127) and 00000001 (1) results in:
A) 10000000 and overflow occurs
B) 10000000 and no overflow
C) 00000000 and overflow occurs
D) 11111111 and no overflow

1

Q. In IEEE 754 single precision, the sign bit indicates:
A) Mantissa length
B) Positive/negative number
C) Exponent value
D) ASCII code

2

Q. What is the output?
int a=5;
int b = a++ + ++a;
printf("%d %d", a, b);
A) 6 11
B) 7 12
C) 7 11
D) 6 12

2

Q. Incremental compilation/testing means:
A) Write full code then test once
B) Compile and test small parts step-by-step as you build
C) Only test with large inputs
D) Avoid debugging tools

2

Q. If AP has first term a=2 and common difference d=3, the 5th term is:
A) 11
B) 14
C) 17
D) 20

2

Q. To test whether n is prime efficiently (basic method), we should check divisors up to:
A) n
B) n/2
C) root(n)
D) 2n

3

Q. To extract digits left to right (e.g., 543 -> 5,4,3) without reversing, you must first:
A) Use %10 repeatedly
B) Find the highest power of 10 ≤ n
C) Convert to binary
D) Use switch-case

2

Q. What is the output?
int n=121, rev=0, t=n;
while(t>0){ rev=rev*10 + t%10; t/=10; }
printf("%s", (rev==n) ? "PAL" : "NOT");
A) PAL
B) NOT
C) 121
D) 0

1

Q. continue in a loop:
A) terminates the loop immediately
B) skips remaining statements of current iteration
C) exits the program
D) repeats the previous iteration

2

Q. In base conversion (decimal to binary), repeatedly divide by 2 and collect:
A) Quotients
B) Remainders
C) Products
D) Averages

2

Q. What is the output?
int x=2;
switch(x){
case 1: printf("A");
case 2: printf("B");
case 3: printf("C"); break;
default: printf("D");}
A) B
B) BC
C) ABC
D) BD

2

Q. A number is Armstrong (3-digit) if it equals:
A) sum of its digits
B) product of its digits
C) sum of cubes of its digits
D) sum of squares of its digits

3

Q. What is the main issue in this sentinel loop?
int x, sum=0, count=0;
while(x!=-1){
scanf("%d",&x);
sum += x;
count++;}
A) scanf cannot be used in loops
B) x is uninitialized and sentinel (-1) is included in sum/count
C) sum must be float
D) while loop never executes

2

Q. else-if ladder is used when:
A) only one condition exists
B) multiple mutually exclusive conditions exist
C) only loops are needed
D) arrays are required

2

Q. A palindrome number means:
A) divisible by 2
B) reads same forward and backward
C) sum of digits is prime
D) has even number of digits

2

Q. A function prototype is mainly used to:
A) allocate memory to function
B) inform compiler about function's return type and parameters
C) stop recursion
D) print function output

2

Q. What will happen with this code?
int fact(int n){
return n * fact(n-1);}
A) Correct factorial
B) Compile-time error always
C) Infinite recursion / stack overflow (no base case)
D) Returns 0 always

3

Q. Binary search requires the array to be:
A) reversed
B) sorted
C) of even size
D) 2D array

2

Q. In bubble sort, number of swaps for array [4,3,2,1] is:
A) 3
B) 4
C) 6
D) 9

3

Q. Correct way to swap using pointers in C is:
A) swap(a,b)
B) void swap(int *x,int *y){int t=*x;*x=*y;*y=t;}
C) void swap(int x,int y){x=y;}
D) swap(&x,&y,&t)

2

Q. What is wrong here?
int a[5]={1,2,3,4,5};
for(int i=0;i<=5;i++) printf("%d ", a[i]);
A) Array cannot be printed in loop
B) Off-by-one: accesses a[5] out of bounds
C) printf needs %f
D) i must start from 1

2

Q. strlen("CSE") returns:
A) 2
B) 3
C) 4
D) 0

2

Q. What is the output?
int a=2,b=3,c=4;
a = b = c;
printf("%d %d %d", a,b,c);
A) 2 3 4
B) 4 3 4
C) 4 4 4
D) 3 4 4

3

Q. Matrix multiplication A(m×n) × B(p×q) is possible only if:
A) m=q
B) n=p
C) m=p
D) n=q

2

Q. A program crashes due to segmentation fault. Best debugging action is:
A) Add more printf everywhere only
B) Use debugger breakpoints + watch variables / check pointer values
C) Rewrite in another language
D) Delete all functions

2

Q. What is the output?
int i,j,c=0;
for(i=1;i<=4;i++){
for(j=1;j<=4;j++){
if(j==i) continue;
if(i+j==6) break;
c++;}}
printf("%d", c);
A) 8 
B) 9 
C) 10 
D) 11

2

Q. What is the output?
int x=0,y=1;
if(x)
if(y) printf("A");
else printf("B");
printf("C");
A) AC 
B) BC 
C) C 
D) ABC

3

Q. What is the output?
int n=2;
switch(n){
case 1: printf("1");
case 2: printf("2");
case 3: printf("3"); break;
default: printf("D");}
A) 2 
B) 23 
C) 123 
D) 2D

2

Q. What is the output?
int a=0,b=0;
if(a++ && ++b) { }
printf("%d %d", a,b);
A) 0 1 
B) 1 1 
C) 1 0 
D) 0 0

3

Q. What is the output?
int i=0;
while(++i<5){
if(i%2==0) continue;
printf("%d", i);}
A) 1234 
B) 13 
C) 24 
D) 135

2

Q. What is the output?
int i, s=0;
for(i=1; i<=4; i++, s+=i) ;
printf("%d", s);
A) 10 
B) 14 
C) 15 
D) 9

2

Q. What is the output?
int a[]={1,2,3,4,5};
int i=0, sum=0;
while(i<5){
sum += a[i];
i += a[i]%2;
i++;}
printf("%d", sum);
A) 6 
B) 7 
C) 8 
D) 9

4

Q. What is the output?
int f(int n){
if(n<=1) return 1;
return n * f(n-2);}
int main(){
printf("%d", f(6));
}
A) 36 
B) 48 
C) 72 
D) 120

2

Q. What is the output?
char s[]="CSE";
printf("%zu %zu", sizeof(s), strlen(s));
A) 3 3 
B) 4 4 
C) 4 3 
D) 3 4

3

Q. What is the output?
int a=1,b=1,c=1;
int r = a++ && ++b || c++;
printf("%d %d %d %d", r,a,b,c);
A) 1 2 2 2 
B) 1 2 2 1 
C) 0 2 2 2 
D) 1 1 2 1

2

